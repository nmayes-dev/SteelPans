@inject MidiPlaybackService Playback
               
<ModalPopup TPayload="Configuration"
            Id="ConfigConflict"
            Title="Loading Layout For Different MIDI"
            ShowCloseButton="false"
            CloseButton="Cancel"
            ConfirmButton="Confirm"
            ConfirmButtonClass="btn-danger"
            OnConfirm="OnConfirmAsync" />

@code {
    private async Task OnConfirmAsync(Configuration? payload)
    {
        if (payload is null)
            return;

        await Playback.LoadPanLayoutAsync(payload.Layout);
    }
}
